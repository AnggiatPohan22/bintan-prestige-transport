---
import ImageCard from "@components/core/ImageCard.astro";

interface Props {
  title: string;
  description?: string;
  image: string;
  alt: string;
}

const props: Props = Astro.props;
---

<ImageCard {...props} />
